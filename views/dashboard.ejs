<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard – StudyMatch</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            background: var(--color-bg);
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 20% 30%, rgba(0, 113, 227, 0.08) 0%, transparent 50%),
                        radial-gradient(circle at 80% 70%, rgba(118, 75, 162, 0.08) 0%, transparent 50%);
            z-index: 0;
            animation: gradient 20s ease infinite;
            background-size: 200% 200%;
        }

        .header-container {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        header {
            max-width: 1200px;
            margin: 0 auto;
            padding: 16px 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 21px;
            font-weight: 600;
            color: var(--color-text-primary);
            text-decoration: none;
            letter-spacing: -0.022em;
            background: var(--gradient-accent);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        header nav {
            display: flex;
            gap: 8px;
        }

        header nav a {
            padding: 8px 16px;
            font-size: 17px;
            color: var(--color-text-secondary);
            border-radius: var(--radius-full);
            transition: all var(--transition-fast);
        }

        header nav a:hover {
            color: var(--color-text-primary);
            background: rgba(255, 255, 255, 0.05);
        }

        main {
            padding: 100px 24px 80px;
            position: relative;
            z-index: 1;
        }

        .dashboard-container {
            max-width: 1200px;
            margin: 0 auto;
            animation: fadeIn 0.6s ease-out;
        }

        .welcome-card {
            background: var(--gradient-accent);
            border-radius: var(--radius-xl);
            padding: 48px;
            margin-bottom: 32px;
            box-shadow: var(--shadow-xl);
            position: relative;
            overflow: hidden;
        }

        .welcome-card::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            animation: pulse 4s ease-in-out infinite;
        }

        .welcome-card h1 {
            font-size: 48px;
            margin-bottom: 8px;
            color: white;
            position: relative;
            z-index: 1;
        }

        .welcome-card p {
            font-size: 21px;
            color: rgba(255, 255, 255, 0.9);
            position: relative;
            z-index: 1;
        }

        .filters-panel {
            background: var(--color-surface);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: var(--radius-xl);
            padding: 32px;
            margin-bottom: 32px;
            box-shadow: var(--shadow-md);
        }

        .filters-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
        }

        .filters-header h2 {
            font-size: 28px;
            color: var(--color-text-primary);
        }

        .clear-filters {
            padding: 8px 16px;
            font-size: 15px;
            background: transparent;
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: var(--color-text-secondary);
            border-radius: var(--radius-full);
            cursor: pointer;
            transition: all var(--transition-base);
        }

        .clear-filters:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 255, 255, 0.3);
            color: var(--color-text-primary);
        }

        .filters-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
        }

        .filter-group label {
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 8px;
            color: var(--color-text-primary);
        }

        .filter-group input,
        .filter-group select {
            padding: 12px 16px;
            font-size: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: var(--radius-md);
            background: var(--color-bg-secondary);
            color: var(--color-text-primary);
            transition: all var(--transition-base);
        }

        .filter-group input:focus,
        .filter-group select:focus {
            outline: none;
            border-color: var(--color-accent);
            background: var(--color-bg-tertiary);
            box-shadow: 0 0 0 4px rgba(0, 113, 227, 0.1);
        }

        .filter-group input::placeholder {
            color: var(--color-text-tertiary);
        }

        .filter-actions {
            display: flex;
            gap: 12px;
            margin-top: 24px;
        }

        .filter-actions button {
            padding: 12px 24px;
            font-size: 17px;
            border-radius: var(--radius-full);
            border: none;
            cursor: pointer;
            transition: all var(--transition-base);
        }

        .btn-apply {
            background: var(--color-accent);
            color: white;
        }

        .btn-apply:hover {
            background: var(--color-accent-hover);
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

        .btn-reset {
            background: transparent;
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: var(--color-text-secondary);
        }

        .btn-reset:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 255, 255, 0.3);
            color: var(--color-text-primary);
        }

        .active-filters {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 16px;
            padding-top: 16px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .active-filter-tag {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 6px 12px;
            background: var(--color-bg-secondary);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: var(--radius-full);
            font-size: 13px;
            color: var(--color-text-secondary);
        }

        .active-filter-tag .remove {
            cursor: pointer;
            color: var(--color-text-tertiary);
            transition: color var(--transition-fast);
        }

        .active-filter-tag .remove:hover {
            color: var(--color-text-primary);
        }

        .users-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 24px;
            margin-top: 32px;
        }

        .user-card {
            background: var(--color-surface);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: var(--radius-xl);
            padding: 32px;
            box-shadow: var(--shadow-md);
            transition: all var(--transition-base);
            position: relative;
            overflow: hidden;
        }

        .user-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-accent);
            transform: scaleX(0);
            transition: transform var(--transition-base);
        }

        .user-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-xl);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .user-card:hover::before {
            transform: scaleX(1);
        }

        .user-card h3 {
            font-size: 24px;
            margin-bottom: 20px;
            padding-bottom: 16px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            text-align: center;
            color: var(--color-text-primary);
        }

        .user-info {
            margin-bottom: 24px;
        }

        .user-info p {
            font-size: 17px;
            margin: 12px 0;
            color: var(--color-text-secondary);
        }

        .user-info strong {
            display: inline-block;
            min-width: 120px;
            font-weight: 600;
            color: var(--color-text-primary);
        }

        .user-card form {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .user-card button {
            width: 100%;
            padding: 12px 22px;
            font-size: 15px;
            font-weight: 400;
            border-radius: var(--radius-full);
            border: 1px solid var(--color-accent);
            background: var(--color-accent);
            color: white;
            cursor: pointer;
            transition: all var(--transition-base);
        }

        .user-card button:hover:not(:disabled) {
            background: var(--color-accent-hover);
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

        .user-card button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            background: var(--color-bg-secondary);
            border-color: rgba(255, 255, 255, 0.1);
        }

        .empty-state {
            text-align: center;
            padding: 80px 32px;
            color: var(--color-text-secondary);
        }

        .empty-state p {
            font-size: 21px;
        }

        footer {
            padding: 40px 24px;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            background: var(--color-surface);
            position: relative;
            z-index: 1;
        }

        footer p {
            color: var(--color-text-tertiary);
            font-size: 14px;
        }

        @media (max-width: 768px) {
            .dashboard-container {
                margin: 40px auto;
            }

            .welcome-card {
                padding: 32px 24px;
            }

            .welcome-card h1 {
                font-size: 36px;
            }

            .filters-panel {
                padding: 24px;
            }

            .filters-grid {
                grid-template-columns: 1fr;
            }

            .users-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="header-container">
        <header>
            <a href="/" class="logo">StudyMatch</a>
            <nav>
                <a href="/dashboard">Dashboard</a>
                <a href="/preferences">Preferences</a>
                <a href="/friend-requests">Friend Requests</a>
                <a href="/chat">Chat</a>
                <a href="/logout">Logout</a>
            </nav>
        </header>
    </div>

    <main>
        <div class="dashboard-container">
            <div class="welcome-card">
                <h1>Welcome, <%= user.username %></h1>
                <p>Browse other students and find your study partners</p>
            </div>

            <div class="filters-panel">
                <div class="filters-header">
                    <h2>Filters</h2>
                    <% const hasActiveFilters = (typeof filters !== 'undefined' && (filters.courses || filters.study_style || filters.study_format || filters.location || filters.availability)); %>
                    <% if (hasActiveFilters) { %>
                        <a href="/dashboard" class="clear-filters">Clear All</a>
                    <% } %>
                </div>

                <form method="GET" action="/dashboard" id="filterForm">
                    <div class="filters-grid">
                        <div class="filter-group">
                            <label for="courses">Courses</label>
                            <input 
                                type="text" 
                                id="courses" 
                                name="courses" 
                                placeholder="e.g. ECE244, MAT291"
                                value="<%= typeof filters !== 'undefined' ? filters.courses : '' %>"
                            >
                        </div>

                        <div class="filter-group">
                            <label for="study_style">Study Style</label>
                            <select id="study_style" name="study_style">
                                <option value="">All Styles</option>
                                <% if (typeof filterOptions !== 'undefined' && filterOptions.studyStyles) { %>
                                    <% filterOptions.studyStyles.forEach(style => { %>
                                        <option value="<%= style %>" 
                                            <%= (typeof filters !== 'undefined' && filters.study_style === style) ? 'selected' : '' %>>
                                            <%= style.charAt(0).toUpperCase() + style.slice(1).replace(/_/g, ' ') %>
                                        </option>
                                    <% }) %>
                                <% } %>
                            </select>
                        </div>

                        <div class="filter-group">
                            <label for="study_format">Study Format</label>
                            <select id="study_format" name="study_format">
                                <option value="">All Formats</option>
                                <% if (typeof filterOptions !== 'undefined' && filterOptions.studyFormats) { %>
                                    <% filterOptions.studyFormats.forEach(format => { %>
                                        <option value="<%= format %>"
                                            <%= (typeof filters !== 'undefined' && filters.study_format === format) ? 'selected' : '' %>>
                                            <%= format.charAt(0).toUpperCase() + format.slice(1).replace(/_/g, ' ') %>
                                        </option>
                                    <% }) %>
                                <% } %>
                            </select>
                        </div>

                        <div class="filter-group">
                            <label for="location">Location</label>
                            <select id="location" name="location">
                                <option value="">All Locations</option>
                                <% if (typeof filterOptions !== 'undefined' && filterOptions.locations) { %>
                                    <% filterOptions.locations.forEach(location => { %>
                                        <option value="<%= location %>"
                                            <%= (typeof filters !== 'undefined' && filters.location === location) ? 'selected' : '' %>>
                                            <%= location.charAt(0).toUpperCase() + location.slice(1).replace(/_/g, ' ') %>
                                        </option>
                                    <% }) %>
                                <% } %>
                            </select>
                        </div>

                        <div class="filter-group">
                            <label for="availability">Availability</label>
                            <input 
                                type="text" 
                                id="availability" 
                                name="availability" 
                                placeholder="e.g. mornings, evenings"
                                value="<%= typeof filters !== 'undefined' ? filters.availability : '' %>"
                            >
                        </div>
                    </div>

                    <div class="filter-actions">
                        <button type="submit" class="btn-apply">Apply Filters</button>
                        <button type="button" class="btn-reset" onclick="window.location.href='/dashboard'">Reset</button>
                    </div>
                </form>

                <% if (hasActiveFilters) { %>
                    <div class="active-filters">
                        <% if (filters.courses) { %>
                            <span class="active-filter-tag">
                                Courses: <%= filters.courses %>
                                <span class="remove" onclick="removeFilter('courses')">×</span>
                            </span>
                        <% } %>
                        <% if (filters.study_style) { %>
                            <span class="active-filter-tag">
                                Style: <%= filters.study_style.replace(/_/g, ' ') %>
                                <span class="remove" onclick="removeFilter('study_style')">×</span>
                            </span>
                        <% } %>
                        <% if (filters.study_format) { %>
                            <span class="active-filter-tag">
                                Format: <%= filters.study_format.replace(/_/g, ' ') %>
                                <span class="remove" onclick="removeFilter('study_format')">×</span>
                            </span>
                        <% } %>
                        <% if (filters.location) { %>
                            <span class="active-filter-tag">
                                Location: <%= filters.location.replace(/_/g, ' ') %>
                                <span class="remove" onclick="removeFilter('location')">×</span>
                            </span>
                        <% } %>
                        <% if (filters.availability) { %>
                            <span class="active-filter-tag">
                                Availability: <%= filters.availability %>
                                <span class="remove" onclick="removeFilter('availability')">×</span>
                            </span>
                        <% } %>
                    </div>
                <% } %>
            </div>

            <% if (users.length === 0) { %>
                <div class="empty-state">
                    <p><%= (typeof filters !== 'undefined' && (filters.courses || filters.study_style || filters.study_format || filters.location || filters.availability)) ? 'No users match your filters. Try adjusting your search criteria.' : 'No other users available yet. Check back soon!' %></p>
                </div>
            <% } else { %>
                <section class="users-grid">
                    <% users.forEach(u => { %>
                        <article class="user-card">
                            <h3><%= u.username %></h3>
                            <div class="user-info">
                                <% if (u.courses) { %>
                                    <p><strong>Courses:</strong> <%= u.courses %></p>
                                <% } %>
                                <% if (u.availability) { %>
                                    <p><strong>Availability:</strong> <%= u.availability %></p>
                                <% } %>
                                <% if (u.study_style) { %>
                                    <p><strong>Study Style:</strong> <%= u.study_style %></p>
                                <% } %>
                                <% if (u.study_format) { %>
                                    <p><strong>Format:</strong> <%= u.study_format %></p>
                                <% } %>
                                <% if (u.location) { %>
                                    <p><strong>Location:</strong> <%= u.location %></p>
                                <% } %>
                            </div>
                            <form action="/friend-request" method="POST" style="margin: 0;">
                                <input type="hidden" name="recipient_id" value="<%= u.id %>">
                                <button type="submit" 
                                    <% if (u.friendRequestStatus === 'pending') { %>
                                        disabled
                                    <% } %>
                                >
                                    <% if (u.friendRequestStatus === 'pending') { %>
                                        Friend Request Sent
                                    <% } else if (u.friendRequestStatus === 'accepted') { %>
                                        Friends
                                    <% } else { %>
                                        Send Friend Request
                                    <% } %>
                                </button>
                            </form>
                        </article>
                    <% }) %>
                </section>
            <% } %>
        </div>
    </main>

    <footer>
        <p>&copy; 2024 StudyMatch. Created by a UofT student.</p>
    </footer>
</body>
</html>
