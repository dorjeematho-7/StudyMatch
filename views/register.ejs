<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - StudyMatch</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <main>
        <article style="max-width: 400px; margin: 3rem auto;">
            <h1>Register</h1>

            <% if (typeof errors !== "undefined" && errors.length > 0) { %>
                <ul style="background-color: #fee; border: 1px solid #fcc; border-radius: 5px; padding: 0.75rem 1rem; margin-bottom: 1rem; list-style: none; color: #c33;">
                    <% errors.forEach(error => { %>
                        <li style="margin: 0.25rem 0;"><%= error.message %></li>
                    <% }) %>
                </ul>
            <% } %>

            <form method="POST" action="/register" novalidate>
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>

                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>

                <input type="submit" value="Register">
            </form>
            <p style="text-align: center; margin-top: 1.5rem;">
                Already have an account? <a href="/login">Login</a>
            </p>
        </article>
    </main>
</body>

</html>