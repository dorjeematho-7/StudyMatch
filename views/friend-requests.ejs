<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Friend Requests – StudyMatch</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <style>
        .friend-requests-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 2rem 1rem;
        }
        .page-header {
            text-align: center;
            padding: 2rem;
            background-color: var(--accent-bg);
            border-radius: var(--standard-border-radius);
            margin-bottom: 2rem;
        }
        .page-header h1 {
            margin-bottom: 0.5rem;
        }
        .page-header p {
            color: var(--text-light);
            margin: 0;
        }
        .requests-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
            margin-top: 2rem;
        }
        .request-card {
            border: var(--border-width) solid var(--border);
            border-radius: var(--standard-border-radius);
            padding: 1.5rem;
            background-color: var(--bg);
            display: flex;
            flex-direction: column;
        }
        .request-card h3 {
            margin-top: 0;
            margin-bottom: 1rem;
            padding-bottom: 0.75rem;
            border-bottom: var(--border-width) solid var(--border);
        }
        .request-info {
            flex: 1;
            margin-bottom: 1.5rem;
        }
        .request-info p {
            margin: 0.75rem 0;
        }
        .request-info strong {
            display: inline-block;
            min-width: 120px;
            font-weight: 600;
        }
        .request-actions {
            display: flex;
            gap: 1rem;
        }
        .request-actions form {
            flex: 1;
            margin: 0;
        }
        .btn-accept {
            width: 100%;
            padding: 0.75rem;
            background-color: #28a745;
            border: var(--border-width) solid #28a745;
            color: white;
            border-radius: var(--standard-border-radius);
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
        }
        .btn-accept:hover {
            background-color: #218838;
            border-color: #218838;
        }
        .btn-decline {
            width: 100%;
            padding: 0.75rem;
            background-color: #dc3545;
            border: var(--border-width) solid #dc3545;
            color: white;
            border-radius: var(--standard-border-radius);
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
        }
        .btn-decline:hover {
            background-color: #c82333;
            border-color: #c82333;
        }
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: var(--text-light);
        }
        .empty-state p {
            font-size: 1.1rem;
        }
        .flash-message {
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: var(--standard-border-radius);
            border: var(--border-width) solid;
        }
        .flash-success {
            background-color: #d4edda;
            border-color: #c3e6cb;
            color: #155724;
        }
        .flash-error {
            background-color: #f8d7da;
            border-color: #f5c6cb;
            color: #721c24;
        }
        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--accent);
            text-decoration: none;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
            background-color: var(--accent-bg);
            border-bottom: var(--border-width) solid var(--border);
        }
        header nav {
            display: flex;
            gap: 1rem;
        }
        header nav a {
            padding: 0.5rem 1rem;
            text-decoration: none;
            border: var(--border-width) solid transparent;
        }
        header nav a:hover {
            border-color: var(--accent);
        }
        body {
            display: block;
        }
        body > * {
            grid-column: 1;
        }
        @media (max-width: 768px) {
            .request-actions {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <header>
        <a href="/" class="logo">StudyMatch</a>
        <nav>
            <a href="/dashboard">Dashboard</a>
            <a href="/preferences">Preferences</a>
            <a href="/friend-requests">Friend Requests</a>
            <a href="/logout">Logout</a>
        </nav>
    </header>

    <main>
        <div class="friend-requests-container">
            <div class="page-header">
                <h1>Friend Requests</h1>
                <p>View and manage your incoming friend requests</p>
            </div>

            <% if (typeof messages !== 'undefined') { %>
                <% if (messages.success_msg) { %>
                    <div class="flash-message flash-success">
                        <%= messages.success_msg %>
                    </div>
                <% } %>
                <% if (messages.error) { %>
                    <div class="flash-message flash-error">
                        <%= messages.error %>
                    </div>
                <% } %>
            <% } %>

            <% if (typeof incomingRequests === 'undefined' || incomingRequests.length === 0) { %>
                <div class="empty-state">
                    <p>No incoming friend requests at the moment.</p>
                </div>
            <% } else { %>
                <section class="requests-grid">
                    <% incomingRequests.forEach(request => { %>
                        <article class="request-card">
                            <h3><%= request.username %></h3>
                            <div class="request-info">
                                <% if (request.courses) { %>
                                    <p><strong>Courses:</strong> <%= request.courses %></p>
                                <% } %>
                                <% if (request.availability) { %>
                                    <p><strong>Availability:</strong> <%= request.availability %></p>
                                <% } %>
                                <% if (request.study_style) { %>
                                    <p><strong>Study Style:</strong> <%= request.study_style %></p>
                                <% } %>
                                <% if (request.study_format) { %>
                                    <p><strong>Format:</strong> <%= request.study_format %></p>
                                <% } %>
                                <% if (request.location) { %>
                                    <p><strong>Location:</strong> <%= request.location %></p>
                                <% } %>
                            </div>

                            <div class="request-actions">
                                <form action="/accept-friend-request" method="POST" style="margin: 0;">
                                    <input type="hidden" name="sender_id" value="<%= request.sender_id %>">
                                    <button type="submit" class="btn-accept">Accept</button>
                                </form>
                                <form action="/decline-friend-request" method="POST" style="margin: 0;">
                                    <input type="hidden" name="sender_id" value="<%= request.sender_id %>">
                                    <button type="submit" class="btn-decline">Decline</button>
                                </form>
                            </div>
                        </article>
                    <% }) %>
                </section>
            <% } %>
        </div>
    </main>

    <footer>
        <p>&copy; 2024 StudyMatch. Created by a UofT student.</p>
    </footer>
</body>
</html>

